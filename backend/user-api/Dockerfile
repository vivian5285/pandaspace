FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npx prisma generate
RUN mkdir -p logs
ENV NODE_ENV=production
ENV API_TYPE=user
EXPOSE 3000
CMD ["npm", "start"] 